{
  "name": "desktop-schedule-widget",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal Windows desktop schedule widget with deadline sorting and completed task cleanup.",
  "main": "src/main.cjs",
  "scripts": {
    "start": "electron .",
    "preview": "http-server src -p 4173 -c-1"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "http-server": "^14.1.1"
  }
}
